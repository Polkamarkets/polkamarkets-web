const features = {
  // regular: {
  //   enabled: !isTrue(environment.FEATURE_FANTASY)
  // },
  // fantasy: {
  //   enabled: isTrue(environment.FEATURE_FANTASY)
  // }
} as const;

export default features;
